# Selenium Starter Project

This project verifies your Selenium setup by launching Chrome, opening a page, and closing Chrome.

## Setup

1. Create/activate your Python environment.
2. Install dependencies:
   - `pip install -r requirements.txt`

## Run

- `python main.py`

If successful, the script will print the page title and a confirmation that Chrome was closed.
